ORG	0
SJMP	MAIN
ORG	30
MAIN:	MOV	TMOD,#01H;DINH THOI 0,16 BIT
		MOV	A,#7FH
		MOV	B,#7FH
		MOV	R1,#30H
;==============================
	MOV	30H,#40H;NAP DU LIEU VAO RAM(CHINH)
	MOV	31H,#79H
	MOV	32H,#24H
	MOV	33H,#30H
	MOV	34H,#19H
	MOV	35H,#12H
	MOV	36H,#02H
	MOV	37H,#78H
	MOV	38H,#00H
	MOV	39H,#10H
;=============================
	MOV	3AH,#7FH;NAP DU LIEU VAO RAM(PHU)
	MOV	3BH,#3FH
	MOV	3CH,#5FH
	MOV	3DH,#6FH
	MOV	3EH,#77H
	MOV	3FH,#7BH
	MOV	40H,#7DH
;=============================
LOOP2:MOV 	R0,#30H
LOOP:MOV		A,@R0
	ACALL	DELAY_1S
	INC	R0
	CJNE	R0,#3AH,LOOP
	MOV		A,#0FFH
	INC	R1
	MOV	 	B,@R1   
	ACALL	DELAY_1S
	CJNE	R1,#3AH,LOOP2
	MOV	R1,#30H
	SJMP	LOOP2
DELAY_1S:
	MOV	R3,#10
LAP3:CLR	0A0H
	SETB	0A1H	
	MOV	P0,A
	ACALL	DELAY
	SETB	0A0H
	CLR		0A1H
	MOV	P0,B
	ACALL	DELAY
	SETB	0A0H
	SETB	0A1H
	DJNZ R3,LAP3
	SETB	0A0H
	SETB	0A1H
	RET
DELAY:
	MOV	TH0,#-200
	MOV	TL0,#-200
	SETB	TR0
WAIT:	JNB	TF0,WAIT
	CLR	TF0
	RET
	  END